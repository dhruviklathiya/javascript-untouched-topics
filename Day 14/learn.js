// import.meta (Module Metadata)

// Gives details about the module itself.

console.log(import.meta.url);


// 🧠 What is import.meta?
// In JavaScript ES modules, import.meta is an object that contains metadata about the current module — basically, information that helps you understand or interact with the module at runtime.

// It's not available in CommonJS (like Node.js require() style by default) — it's specific to ES Modules (ESM).